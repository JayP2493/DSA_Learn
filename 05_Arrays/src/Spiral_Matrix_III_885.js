const spiralMatrixIII = function(rows, cols, rStart, cStart) {
    let ansArr = [];
    let rEnd = rStart+1;
    let cEnd = cStart+1;
    let length = 1;
    let cPos = [cStart,cEnd]

    while (ansArr.length < (rows*cols)) {

    }
};